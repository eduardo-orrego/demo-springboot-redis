spring:
    application:
        name: redis-cache
    cache:
        type: redis
    redis:
        host: localhost
        port: 6379
        timeout: 60000
    threads:
        virtual:
            enabled: true
server:
    port: 8087